const userName: string = 'Lufra';

const sum = (a: number, b: number) => {
  return a + b;
}

sum(2, 2);

class Person {
  constructor(age: number, lastName: string) {
  }
}

const lufra = new Person(27, 'Rossi');
